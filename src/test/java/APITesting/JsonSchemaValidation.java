package APITesting;

public class JsonSchemaValidation {
	
	
	
	/*
	 * In schema validation we check the type of the data in JSON response. 
	 * For example; In JSON response there is many field in the response array. In
	 * schema validation we will check whether the type of different field is the
	 * correct or not?
	 */
			

}
